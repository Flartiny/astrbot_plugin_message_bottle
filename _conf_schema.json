{
    "max_text_length": {
        "description": "瓶中信文字内容的最大长度限制",
        "type": "int",
        "default": 500,
        "hint": "超过此长度的瓶中信将被拒绝"
    },
    "max_images": {
        "description": "每个瓶中信允许的最大图片数量",
        "type": "int",
        "default": 1,
        "hint": "超过此数量的图片将被忽略"
    },
    "api_base_url": {
        "description": "MongoDB api_base_url",
        "type": "string",
        "default": "https://adb.flartiny.top",
        "hint": "MongoDB api_base_url"
    },
    "use_base64": {
        "description": "是否使用base64编码图片",
        "type": "bool",
        "default": false,
        "hint": "开启略微增加存储开销，关闭可能面临图片过期"
    },
    "enable_content_safety": {
        "description": "是否启用内容安全检查",
        "type": "bool",
        "default": false,
        "hint": "这是一个独立开关，其他内容在'配置文件-百度内容审核配置'中设置"
    }
}